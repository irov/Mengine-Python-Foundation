#include <metal_stdlib>
using namespace metal;

struct VertexIn {
    float3 position [[attribute(0)]];
    float4 color [[attribute(1)]];
    float2 uv0 [[attribute(2)]];
};

struct VertexOut {
    float4 position [[position]];
    float4 v_Col;
    float2 v_UV0;
};

vertex VertexOut vertexShader(VertexIn in [[stage_in]],
                              constant float4x4 &wvpMatrix [[buffer(1)]]) {
    VertexOut out;
    out.position = wvpMatrix * float4(in.position, 1.0);
    out.v_Col = in.color;
    out.v_UV0 = in.uv0;
    return out;
}
